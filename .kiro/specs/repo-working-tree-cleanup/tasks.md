# Tasks: Repo Working-Tree Cleanup

1. [ ] Commit A: stage delete `apps/portal/app/`, add `_app_legacy_shadow/`.
2. [ ] Commit C: remove `ai-backends/`.
3. [ ] Commit E: `git mv features _features_legacy_shadow`; exclude in tsconfig; fix jest coverage paths.
4. [ ] Commit B: finalize hub route move to `src/app/hub/`.
5. [ ] Commit D: remaining portal product edits under `apps/portal/src` + related portal config.
6. [ ] Commit F: packages/theme, eslint-config, root lockfile/package/turbo.
7. [ ] Commit G: agent/docs/hooks (`.cursor`, `.kiro`, `.husky`, AGENTS/CLAUDE).
8. [ ] Verify portal `:3000` /login; run scoped quality; sceptic + alignment score.
