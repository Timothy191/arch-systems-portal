# Product ↔ Agentic AI Layer Split — Tasks

Ordered units (retroactive — completed in session 2026-07-20).

- [x] 1. Audit product→AI imports and soft couplings (`dev.sh`, Prettier, ESLint)
- [x] 2. Remove `scripts/dev.sh` → `ai.sh` probe
- [x] 3. Expand `.prettierignore` + `.eslintignore` for agentic surfaces
- [x] 4. Author `.cursor/standards/layer-boundary/STANDARD.md`
- [x] 5. Update `AGENTS.md` § Two Layers + `CLAUDE.md` / `00-global-alignment.mdc` pointers
- [x] 6. Smoke: rename `.cursor` → portal `type-check` + `build` (both exit 0)
- [x] 7. `pnpm ai check` PASS; sceptic technical SHIP
- [ ] 8. Optional: move `@openrouter/*` / agent scripts off root install path
- [ ] 9. Optional: expand ops-gateway taxonomy note in STANDARD (product ops, agent-facing MCP)
