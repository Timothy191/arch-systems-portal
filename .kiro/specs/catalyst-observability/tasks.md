# Tasks: Catalyst observability

1. [x] Specs (this folder)
2. [x] Env schema + `.env.example` + write `.env.local` from `inf` session (gitignored)
3. [x] `catalyst-gateway.ts` + tests
4. [x] Wire Gemini + cloud Ollama
5. [x] Add `@inference/tracing`; register in `instrumentation.ts`; span `invokeAiChat`
6. [x] type-check + portal tests + keepalive
