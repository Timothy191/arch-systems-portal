# Tasks: Auto-Formatter Background Service

- [x] 1. Define auto-formatter background subagent entry at `.cursor/agents/auto-formatter.md`.
- [x] 2. Create executable helper script at `.cursor/agents/auto-formatter/scripts/run-format.sh`.
- [x] 3. Register auto-formatter subagent in `.cursor/agents/README.md`.
- [x] 4. Validate surface integrity via `pnpm ai check`.
- [x] 5. Verify format compliance with `pnpm format:check` and full quality check with `pnpm quality`.
