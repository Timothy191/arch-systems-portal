# Arch Official Palette — Tasks

- [x] T1. Create `palette.css` with complete `--palette-*` primitives
- [x] T2. Import palette.css before variables.css in theme index
- [x] T3. Rewire `variables.css` arch/glass/os-shell/semantic tokens to palette
- [x] T4. Add `tokens/palette.ts` + export `OFFICIAL_PALETTE` from `@repo/theme`
- [x] T5. Expand Tailwind `arch.*` and add `palette.*` namespaces
- [x] T6. Fix `--warning` to use semantic amber
- [ ] T7. Gradual component migration to `palette.*` / `arch.*` utilities (future)
