# Remove Focus Mode — Tasks

1. [x] Remove FocusModeToggle/Provider from layout and hub page (already gone from portal src).
2. [x] Simplify RouteBackground to single permanent wave MP4; purge focus video CSS.
3. [x] Delete unused `useFocusMode`; remove `body.focus-mode` overrides from `@repo/ui` globals.
4. [ ] Verify header has no focus toggle and login/hub still load (manual / quality).
