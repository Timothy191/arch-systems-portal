# Login Brand Banner — Tasks

- [x] T1. Write specs (this folder).
- [x] T2. Download AI SVGs into `apps/portal/public/branding/ai/` and verify non-empty.
- [x] T3. Implement `Marquee` (+ export path) with pause/reverse/reduced-motion.
- [x] T4. Add `LoginBrandBanner` and swap login footer content.
- [x] T5. Verify `/login`, update tests, format, score alignment.

Done when all requirements pass and Alignment ≥ 80.
