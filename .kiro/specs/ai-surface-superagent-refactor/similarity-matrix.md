# Similarity matrix (optimizer summary)

Research + `ai-system-optimizer` findings for AI-surface consolidation. **Do not flatten into god-agents** — prefer orchestrator + specialists (see `.cursor/agents/_shared/references/agent-families.md`).

| Cluster | Decision | Action |
|---|---|---|
| Quality triad (`quality` / `verify` / `verify-changes`) | **ALIAS** | Canonical = `quality`; `verify` = portal; `verify-changes` = GitHub/Copilot → full |
| Healing agency (`agency-lead` + Round 1/2 + healer) | **ABSORB** | Orchestrator docs only under `agency-lead`; keep peer agent entries |
| Layout meta-skills (`skill-layout` / `agent-layout` / `claude-code-layout`) | **KEEP** | Separate standards paths; joint gate via `pnpm ai check` |
| Frontend / portal (`frontend-design` / `frontend-implementer` / `nextjs-fullstack`) | **KEEP** | Different QA bars; family under `nextjs-fullstack` |
| CLI / coding tools (`aider` / `goose` / `omp` / `openspec`) | **KEEP** | Distinct triggers and isolation needs |
| Review / score (`sceptic` / `agent-alignment-score` / `security`) | **KEEP** | Never merge adversarial review with formal score or AppSec |

No further agent merges planned from this matrix.
