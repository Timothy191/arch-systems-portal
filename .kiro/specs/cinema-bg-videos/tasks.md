# Cinema Background Videos — Tasks

1. [x] Write requirements / design / tasks.
2. [x] Download Wikimedia popcorn + cinema source videos (Mixkit CDN blocked).
3. [x] Convert to `light-mode.mp4` and `focused-mode.mp4`; place under `public/background/`.
4. [x] Fix `HeroBackground` path (`light mode.mp4` → `light-mode.mp4`).
5. [x] Remove unused hash `.webm`s; refresh `auth-bg-poster.jpg`.
6. [x] Verify both URLs return HTTP 200 (no 404s).
