# Arch Start Menu — Tasks

- [x] T1. Specs written.
- [x] T2. MacMenuBar `appMenu` slot + default fallback.
- [x] T3. ArchStartMenu panel (search, pinned, all apps, lock, logout).
- [x] T4. CommandBar listens for `arch-open-command-bar`.
- [x] T5. Wire slot in `layout.tsx` via ArchMacMenuBar + ArchLockOverlay.
- [x] T6. Tests + sceptic + alignment score.
