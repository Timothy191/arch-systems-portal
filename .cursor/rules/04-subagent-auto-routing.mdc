---
description: Auto-route user tasks to project subagents in .cursor/agents — mandatory delegation
alwaysApply: true
---

# Subagent Auto-Routing (Mandatory)

Project subagents live in `.cursor/agents/`. When a user task matches a row below, **automatically delegate** via the Task tool / subagent invocation. Do not silently do the specialist's job in the parent thread when a matching agent exists.

## Routing table

| User task signals | Auto-use agent | Timing | Anti-triggers (do not use for) |
|---|---|---|---|
| Non-trivial multi-step work, map critical aspects/gaps/handoffs, pre-flight outline, "outline first", scout scope before specialists | `fast-outliner` | **Before** other agents | Implementation, visual design, docs sync, idle fill, adversarial review |
| Landing/branded/marketing UI, hero, first viewport, visual composition, "looks AI", brand hierarchy | `frontend-design` | **Before** implementing UI | App Router plumbing, CRUD without branded need, docs sync, outlining, sceptic review |
| Portal pages/components/forms, App Router, Server Actions UI, Tailwind in `apps/portal`, a11y wiring | `frontend-implementer` | **When** building/changing portal UI | Branded first-viewport composition, docs sync, outlining, idle work, sceptic |
| Skills, rules, agents, hooks, AGENTS drift, CLAUDE.md, wiki, docs sync | `ai-docs-sync` | **When** auditing/changing AI/docs surfaces | Product UI, visual design, formal alignment score, adversarial code review |
| Review, reality-check, "is this done", verify fix, PR/diff critique, production readiness, sceptic/skeptic | `sceptic` | **After** non-trivial changes, **before** claiming done | Formal score emission, UI implementation, docs sync, outlining |
| Independent side work while blocked/waiting, parallel idle tasks, fill wait time | `idle-runner` | **While** other agents wait/blocked | Critical path files, main feature implementation, marking done, quality/score |
| Vercel, eve, v0 logos, Geist Brands, partner marks in taskbar/login chrome | `vercel-brand-sync` | **When** auditing/updating Vercel-family brand assets | General UI impl, docs sync, sceptic |
| OpenSpec change proposal / validate / status / instructions / archive under `openspec/` | `openspec` | **When** multi-file change lifecycle or user names OpenSpec | Portal paint, branded hero, pure lint |
| Bounded surgical edit on known files, one-shot pair fix via Aider | `aider` | **When** file set is known and edit is bounded | Open research, MCP recipes, large refactor, design |
| Goose recipes / MCP multi-tool automation / `goose run` | `goose` | **When** recipe or research→act automation | Tiny single-file edit, OpenSpec-only validate |
| Large refactor / deep multi-file coding via omp | `omp` | **When** aider too narrow for the coding scope | Spec-only, brand, docs drift |
| Repeatable procedure after 3+ successes, 5+ tool-call task, error recovery, user correction, or Alignment FAIL with pattern | `skill-self-improve` **skill** | **After** done or FAIL (parent runs skill) | Inventing AGENTS policy; duplicate skills; replacing sceptic/score |
| **Every user prompt** — background layout/sync/dedupe; parent awaits on AI-surface work | `ai-maintenance-checker` | **Start** of prompt (background) | Product features, AGENTS policy edits, ai-docs-sync rewrites, sceptic |

## Rules

1. **Match → delegate.** Prefer the specialist; parent synthesizes results.
2. **Multiple matches → parallel.** e.g. user prompt → `ai-maintenance-checker` (background) + task specialists; large task = `fast-outliner` first; after code = `sceptic`; while blocked = `idle-runner`.
3. **Always launch `ai-maintenance-checker`** at prompt start (background). Await before final reply when AI surfaces were touched. See `.cursor/rules/06-ai-maintenance-background.mdc`.
4. **Always run `sceptic`** before saying done on multi-file or production-facing work (unless user explicitly skips review).
5. **Formal Alignment Score** → `agent-alignment-score` skill after sceptic (or when scoring requested). Include extended fields (Code quality, Pro bar, Tokens saved, 3 actions, Adaptive next).
6. **Unified AI check** → `pnpm ai` (not scattered validate scripts).
7. **Adaptive distill** → when Adaptive next or Hermes criteria match → `skill-self-improve` skill (see `.cursor/rules/07-adaptive-skill-loop.mdc`).
8. **Descriptions win.** Each agent's `description` frontmatter is the trigger source of truth.
9. **Do not invent agents.** Only route to files under `.cursor/agents/` (skills are separate).
10. **User override.** If the user names an agent, use that agent even if another also matches.

## Inventory (keep in sync)

| Agent | Entry | Collateral | Anti-triggers (summary) |
|---|---|---|---|
| fast-outliner | `fast-outliner.md` | `fast-outliner/` | No implementation, design, docs, idle, sceptic |
| frontend-design | `frontend-design.md` | `frontend-design/` | No plumbing, CRUD-only, docs, outline, sceptic |
| frontend-implementer | `frontend-implementer.md` | `frontend-implementer/` | No branded hero, docs, outline, idle, sceptic |
| ai-docs-sync | `ai-docs-sync.md` | `ai-docs-sync/` | No UI, design, formal score, sceptic |
| sceptic | `sceptic.md` | `sceptic/` | No formal score, UI, docs, outline |
| idle-runner | `idle-runner.md` | `idle-runner/` | No critical path, main feature, done-claims |
| ai-maintenance-checker | `ai-maintenance-checker.md` | `ai-maintenance-checker/` | No product features, policy edits, docs rewrites |
| vercel-brand-sync | `vercel-brand-sync.md` | `vercel-brand-sync/` | No general UI, docs sync, sceptic |
| openspec | `openspec.md` | `openspec/` | No portal paint, brand hero, pure lint |
| aider | `aider.md` | `aider/` | No research/MCP, large refactor, design |
| goose | `goose.md` | `goose/` | No tiny single-file, OpenSpec-only, brand |
| omp | `omp.md` | `omp/` | No spec-only, brand, docs drift |

Layout standard: `.cursor/standards/agent-layout/STANDARD.md`  
AI system command: `pnpm ai` · `.cursor/standards/ai-system/STANDARD.md`  
Shared refs: `.cursor/agents/_shared/references/`

When adding a new project agent, update this table in the same change.
