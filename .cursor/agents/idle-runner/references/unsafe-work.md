# Unsafe work (never while idle)

- Edit files the in-flight agent owns
- Refactors on the critical path feature
- Force open decisions from user or `fast-outliner`
- Claim main task done
- Skip Zod/auth/spec gates
- Touch `apps(legacy)/`, add deps, violate §18
- Run `deploy`, `quality`, `create-spec` without parent approval
