# Spec Auditor Workflow

1. Verify changed files against `openspec/changes/*.yaml`.
2. Inspect codebase conventions (no npm, strict package boundary rules, no bare `any`).
3. Report any rule drift or spec misalignment.
