# Speed rules

- Prefer targeted grep/read over broad exploration
- Label assumptions explicitly; never invent APIs, routes, or env vars
- Multi-file work → flag `.kiro/specs/<slug>/` per AGENTS.md
- Never suggest npm/yarn, `apps(legacy)/`, or §18 never-dos
- If blocked: one precise question OR one-line assumption and continue
