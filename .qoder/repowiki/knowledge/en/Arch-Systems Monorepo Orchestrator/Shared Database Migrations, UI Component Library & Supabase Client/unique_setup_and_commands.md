`@repo/database`: run `pnpm supabase:dev` to spin up the local Supabase stack, then `pnpm supabase:push` to apply migrations or `pnpm test:migration-rollback` / `test:security:p0-signup-role` to validate idempotency and RLS. `@repo/supabase`: `pnpm supabase:init` / `supabase:link --project-ref <id>` / `supabase:pull` to sync remote schema back into this package's `migrations/` and `src/database.types.ts`.