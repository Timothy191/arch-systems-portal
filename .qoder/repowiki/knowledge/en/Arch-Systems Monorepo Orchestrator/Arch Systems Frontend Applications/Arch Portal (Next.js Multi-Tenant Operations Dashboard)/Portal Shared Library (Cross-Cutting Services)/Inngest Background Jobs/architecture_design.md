Each file exports a single `InngestFunction.Any` registered via `inngest.createFunction`, with one handler per responsibility: embedding-generation (AI text→vector), memory-persist (durable fallback for assistant response persistence after stream termination), report-generation (shift-report aggregation from daily/production logs), and sync-playback (idempotent replay of ADD_BREAKDOWN / RESOLVE_BREAKDOWN / ADD_SAFETY_INCIDENT / ADD_DAILY_LOG actions). All handlers depend inward on shared portal libs (`@/lib/errors/error-logger`, `@/lib/observability/metrics`) and external services via `@repo/supabase/server` and `@repo/utils/inngest`; they do not import each other. Triggers are event-driven — custom events like `aiGenerateEmbeddingEvent` / `aiMemoryPersistEvent` or string events like `reports/generate` and `sync/playback`. Idempotency is enforced at the DB level using an `idempotency_key` column checked before inserts in the playback handler.