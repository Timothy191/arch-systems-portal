# Verify → Quality Alias

| Skill     | Mode   | Script                                                  |
| --------- | ------ | ------------------------------------------------------- |
| `quality` | full   | `quality/scripts/run-full.sh`                           |
| `verify`  | portal | `verify/scripts/run-portal.sh` (same as quality portal) |

Identical gold contract; only the `Mode:` line differs.
