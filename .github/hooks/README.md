# Git Hooks Configurations (`.github/hooks/`)

This directory houses git hook scripts and automation templates utilized during client-side git events or within CI/CD pipelines to ensure code compliance.

## Hook Templates

- **`pre-commit.sh`**: A template shell script for running linters, tests, and database RLS checks before a commit is finalized.
